1) Name : Balaji Paiyur Mohan
   ID   : 1001576836
2) Java is used
3) The code is structured as per the graph search algorithm. Some important key points are:
	- check if node is goal state, reconstruct the path
	- Else expand and add the node to fringe.
	- Sort the list(fringe) with respect to lowest cumulative cost
	- Add the expanded node to closed set of states
	- Check if the state is in set. If yes, don't expand else expand.
4) Since it's Java language, javac is used to compile and java along with arguments is used to execute the program.